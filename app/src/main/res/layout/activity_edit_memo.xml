<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_edit_memo"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.hyunjunian.www.justamemo.EditMemoActivity">


    <TextView
        android:id="@+id/topbar_edit_memo"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center"
        android:background="@color/colorPrimary"
        android:elevation="1dp"
        android:text="수정 날짜"/>

    <ImageView
        android:id="@+id/bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/topbar_edit_memo"
        android:src="@drawable/bg"
        android:scaleType="centerCrop" />

    <EditText
        android:id="@+id/field_edit_body"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/topbar_edit_memo"
        android:padding="10dp"
        android:gravity="start"
        android:inputType="textMultiLine"
        android:background="@null"
        android:scrollHorizontally="false"
        android:hint="메모 작성..." />

    <Button
        android:id="@+id/submit_edit_memo"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:background="@color/colorPrimary"
        android:textColor="@color/colorPrimaryDark"
        android:text="완료"/>

    <TextView
        android:id="@+id/delete_memo"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:padding="10dp"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:textColor="@color/danger"
        android:text="메모 삭제"/>

</RelativeLayout>
